<div data-controller="hello">

  <%= form_with(model: project, id: 'form') do |form| %>
    <% if project.errors.any? %>
      <div style="color: red">
        <h2><%= pluralize(project.errors.count, "error") %> prohibited this project from being saved:</h2>
        <ul>
          <% project.errors.each do |error| %>
            <li><%= error.full_message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>

    <div class = 'form-group'>
      <%#= form.label :name, style: "display: block" %>
      <%= form.text_field :name, required: true, class: 'form-control mb-2', placeholder: 'name' %>
    </div>

    <div class="form-group">
      <%#= form.label :description, style: "display: block" %>
      <%= form.text_field :description, required: true, class: 'form-control mb-2', placeholder: 'description' %>
    </div>

    <div class = 'text-center'>
      <%#= f.submit 'Save', class: 'btn btn-add' %>
      <%= form.submit 'Save', class: 'btn btn-primary pull-right' %>
      <!-- <button data-action="click->hello#greet", class="btn-btn-info"> -->
      <!-- Greet -->
    <!-- </button> -->
    </div>
  <% end %>
</div>

<style>
.error{
  color: red;
}
</style>